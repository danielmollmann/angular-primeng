import { Routes } from '@angular/router';

export const routes: Routes = [
    {
        path: '',
        redirectTo: 'navbar', // ou outra rota válida
        pathMatch: 'full',
    },
    // A rota para Pruebas foi removida
];
